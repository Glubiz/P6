import { Group } from './Group';
import { Node } from './Node';
export interface GossiperOptions<V extends object> {
    /**
     * The interval to gossip with.
     */
    intervalInMs: number;
    gossip: (node: Node<V>) => void;
}
export declare class Gossiper<V extends object> {
    private readonly group;
    private readonly timer;
    private readonly gossip;
    constructor(group: Group<V>, options: GossiperOptions<V>);
    destroy(): void;
    private findAndGossip;
}
//# sourceMappingURL=Gossiper.d.ts.map