import { PeerMessageType, PeerMessage } from '../messages';
/**
 * Encode a peer message into a binary format.
 *
 * @param type -
 *   type of peer message
 * @param data -
 *   data of message
 * @returns
 *  buffer with encoded message
 */
export declare function encodeBinaryPeerMessage<T extends PeerMessageType>(type: T, data: PeerMessage<T>): ArrayBuffer;
//# sourceMappingURL=encodeBinaryPeerMessage.d.ts.map